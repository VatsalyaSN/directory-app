.details-wrapper{
    width: calc(100% - 56px);
    height: 100%;
    padding: 0px;
    margin-left: 6px;
    background-color: #fff;
    box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.30);
	border-radius:0.3em;
    overflow: hidden;
}

